name: Deploy To Develop Server

on:
  pull_request:
    branches:
      - main

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: ssh 세팅
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.NCLOUD_PRIVATE_KEY }}

      - name: Github Repository 로드
        uses: actions/checkout@v4

      - name: Create .env file
        run: echo "${{ secrets.APP_ENV }}" > .env

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./api
          push: true
          tags: aprileaf/memory-bucket:latest

      - name: Create Docker Context
        run: docker context create remote --docker host=ssh://${{ NCLOUD_USERNAME }}@${{ NCLOUD_HOST }}:${{ NCLOUD_PORT }}

      - name: Use Remote Context
        run: docker context use remote

      - name: Docker compose down
        run: docker compose down

      - name: Docker compose pull
        run: docker compose pull

      - name: Docker compose up
        run: docker compose up -d


